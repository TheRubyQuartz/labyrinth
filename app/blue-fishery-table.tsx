import rows from './blue-fishery-table.json';
export function BlueFisheryTable(){return <section id="blue-historical-fishery-table" className="crab-section crab-level-1"><h3>Historical fishery value · 2000–2013</h3><p>Values reproduced from the supplied blue-crab article. Dollar values are millions of US dollars; percentages are shares of national harvest weight, not revenue.</p><div style={{overflowX:'auto'}} tabIndex={0} role="region" aria-label="Historical fishery value table"><table className="observation-table"><caption>Blue-crab fisheries: value and national harvest-weight share</caption><thead><tr><th scope="col">Year</th>{['Maryland','Virginia','North Carolina','Louisiana'].map(state=><th scope="col" key={state}>{state}<br/>Value / share</th>)}<th scope="col">USA value</th></tr></thead><tbody>{rows.map(row=><tr key={row[0]}><th scope="row">{row[0]}</th>{[1,3,5,7].map(i=><td key={i}>{row[i]} / {row[i+1]}</td>)}<td>{row[9]}</td></tr>)}</tbody></table></div><p className="subnote">The supplied prose and table disagree on some Louisiana percentages (including 2002, 2009 and 2012). Both are preserved as supplied, not reconciled or treated as current estimates. <a href="https://en.wikipedia.org/wiki/Callinectes_sapidus">Source: Callinectes sapidus</a>.</p></section>}
